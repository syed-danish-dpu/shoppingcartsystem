server:
  port: 8080

spring:
  application:
     name: Apigateway
  cloud:
    gateway:
      routes:
      - id: Profile
        uri: http://localhost:8081/
        predicates:
        - Path=/userDetails/**
 
